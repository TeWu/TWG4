<%= form_for(photo_in_album) do |f| %>
  <% if photo_in_album.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(photo_in_album.errors.count, "error") %> prohibited this photo_in_album from being saved:</h2>

      <ul>
      <% photo_in_album.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :photo_id %>
    <%= f.text_field :photo_id %>
  </div>

  <div class="field">
    <%= f.label :album_id %>
    <%= f.text_field :album_id %>
  </div>

  <div class="field">
    <%= f.label :display_order %>
    <%= f.number_field :display_order %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
